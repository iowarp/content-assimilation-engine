#include "format/format_factory.h"
#include "repo/repo_factory.h"

// This file now just includes the separate factory implementations
// The actual implementations are in:
// - omni/format/format_factory.cc
// - omni/repo/repo_factory.cc

// No additional code needed here as the implementations are now
// in the separate factory files in format/ and repo/ subdirectories